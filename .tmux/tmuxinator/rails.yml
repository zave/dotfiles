# ~/.tmuxinator/rails.yml
project_name: rails
project_root: ~/Git/<%= @args[0] %>
windows:
  - rails:
      layout: main-vertical
      panes:
        - vim .
        - bundle exec rails s
        - bundle exec rails c
        - #empty, will just run plain bash
